services:
  docs:
    image: hutech_program_local_docs
    container_name: hutech_program_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./hutech_program:/app/hutech_program:z
    ports:
      - '9000:9000'
    command: /start-docs
